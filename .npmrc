# GitHub Packages registry for scoped @gsushmitha packages
# This is for local publishing only. Users consuming the package
# should set up their own .npmrc in their home directory.

@gsushmitha:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GH_TOKEN}
